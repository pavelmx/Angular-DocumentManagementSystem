export class CatalogOfOperationMode {

    id: number;
    operationMode: string;

    constructor(id: number, operationMode: string) {
        this.id = id;
        this.operationMode = operationMode;
    }

}